# Feature Status Board

High-level view of where the product stands. **Convention: update this file whenever
a feature ships or changes state.** Statuses: 📋 planned · 🚧 in progress · ✅ shipped.

Scope and acceptance criteria for each feature live in [PRD.md](./PRD.md).

## Engine (calculation core)

| Feature | Status | Notes |
|---|---|---|
| Conductor ampacity + derating (310.16, 310.15, 110.14(C)) | ✅ shipped | 2026-08-15 · golden + property tests |
| Voltage drop + min-size-for-drop (Ch. 9 T8) | ✅ shipped | 2026-08-15 · DC-resistance method; AC impedance (T9) later |
| Standard breaker sizing (240.4, 240.6) | ✅ shipped | 2026-08-15 · incl. next-size-up + small-conductor caps |
| Circuit composer (load → conductor → breaker) | ✅ shipped | 2026-08-15 · `sizeCircuit`, mini-split scenario verified |
| Conduit fill (Ch. 9) | ✅ shipped | 2026-08-16 · golden + property tests; EMT / PVC Sch 40 / LFNC-B, nipple 60%, Note 7; bare EGC later (needs T8 area column) |
| Box fill (314.16) | ✅ shipped | 2026-08-28 · golden + property tests; check + size modes, (B)(1)–(B)(5) incl. EGC quarter rule; Tables 314.16(A)/(B)(1) cross-verified vs two NFPA-licensed reprints |
| Residential load calc (Art. 120, ex-220) | ✅ shipped | 2026-08-28 · both methods (standard + optional 120.82) with per-line bilingual breakdowns, golden + property tests; NEC 2026 moved load calcs to new Art. 120 with 22 VA/m² lighting + 8 kVA optional tier (cross-verified) |
| Grounding (250.66, 250.122) | ✅ shipped | 2026-08-28 · `egcSize` (250.122 + (B)/(D) proportional upsizing) and `gecSize` (Table 250.66 + 250.66(A) rod cap, cross-verified vs zing2/dakotaprep/expertce/ELR) both golden-tested |
| NEC deviations (warn, never block) | ✅ shipped | 2026-08-31 · el NEC es guía que la app enseña, no regla que fuerza: `Deviation { key, en, es, citations?, severity }` en `WithProvenance` (requerido), `mergeDeviations`, `isNonCompliant`; severidades `off-code` / `conditional` / `recommendation` (solo `off-code` marca «No cumple NEC» — la caída de tensión de 210.19 es Nota Informativa, no violación); los throws de mínimos ahora devuelven mejor esfuerzo + deviation (`sa < 2`, `sizeBox`, `sizeConduit`, `minConductorForLoad`, `minSizeForVoltageDrop`, 240.4 vía `protectsConductor`, `sizeCircuit`); `EngineError.kind: 'input' \| 'coverage'` separa «dato no transcrito» de «entrada inválida»; `TemplateWarning.severity`/`citations` + `TemplateEngineCall.probe`; el piso de 100 A de 230.79(C) sigue siendo piso del resultado y NO emite deviation; 297 tests, cero goldens editados |
| A/C circuits per Article 440 (MCA/MOCP) | ✅ shipped | 2026-08-31 · corregidos dos errores que se sumaban: (1) la plantilla aplicaba el 125% de carga continua ENCIMA del MCA de placa, que ya lo incluye (440.4(B)/440.32) — sobredimensionaba un calibre completo de 18k para arriba; (2) el térmico salía del cálculo de carga en vez de la placa: ahora `BreakerInput.mocpA` lo toma del MOCP (440.22) y `240.4(G)` exime al conductor del chequeo general de 240.4, que es lo que permite #14 con térmico de 20 A. Resultado 18k: 12 AWG·15 A → **14 AWG·20 A**. Deviation nueva `mocp-below-required` si la placa se contradice |
| Solar sizing (Art. 690 + panels/inverter/battery) | 📋 planned | |

## Data

| Feature | Status | Notes |
|---|---|---|
| NEC 2026 core tables (310.16, corrections, T8, 240.6) | ✅ shipped | 2026-08-15 · cross-verified vs published sources, 0 mismatches (official 2026 text paywalled; unchanged per change summaries) |
| NEC Ch. 9 Tables 1/4/5 + Table 250.122 | ✅ shipped | 2026-08-16 · cross-verified vs two independent sources each; verification caught + fixed a THW 14–8 AWG row-group error |
| Bilingual citation labels (en/es) | ✅ shipped | 2026-08-15 · 2026-08-17: + plain-language reasons (`reasonEs/reasonEn`, `citationReason()`) for beginner-facing chips |
| Nominal service voltage 220 V | ✅ shipped | 2026-08-31 · `article-220-residential.json` gana `nominalServiceVoltage: 220` (antes 240 fijo en el código): el servicio es nominalmente 120/240 pero en el tablero mide más cerca de 220, y el divisor menor es el conservador — reporta MÁS amperios. La aritmética en VA de los goldens NO cambió; solo los amperios y, en el caso de 167 m², la acometida resultante (110 A → 125 A). La plantilla de A/C gana pregunta de voltaje medido (200–250 V, default 220) |
| Device catalog (wattages, synonyms, photos) | 🚧 in progress | 2026-08-30 · AC nameplate presets (5, MCA/MOCP) + 17 appliance wattage presets (es-SV synonyms, Art. 120 categories); provenance infra live (`verifiedAt`/`source`, WATTAGES.md, shrinking `KNOWN_UNVERIFIED_*` lint, «por verificar» en el panel); UI inputs watts-first (W ≈ VA, breakdown stays VA); spec-sheet run 2026-08-30: 10 entries verified (termo, horno, plancha, lavaplatos, tv, bomba, ac-12k/ac-18k pairs), 9 findings logged as proposals (ducha→5,400?, ac-9k MCA→10?, etc.) pending user nameplate check; photos later |
| El Salvador local-practice notes | ✅ shipped | 2026-08-31 · `reference/local-practice.ts`, indexada por clave de deviation: el único archivo en una ruta de cálculo que nombra un país (el motor se queda neutral). Semillas: 6 AWG del medidor al tablero ≈ 55–65 A vs el mínimo de 100 A, tablero sin interruptor principal (230.70/230.71/408.36), cocina con menos de 2 circuitos, y el piso de 100 A (nota permanente junto a «Acometida sugerida» en el cálculo de carga y en su memoria, porque ahí es donde se lee el número que no coincide con la instalación real). Lint de cobertura contra `DEVIATION_KEYS` para que ninguna nota quede inalcanzable; 4 citas nuevas (`s240_4`, `s230_70`, `s230_71`, `s408_36`) |
| Glossary / regional terminology DB | 🚧 in progress | 2026-08-30 · 103 entries in data (definición + sinónimos es-SV + inglés + artículos NEC), CI coverage lint; «placa» (tapa) vs «placaDeDatos» (nameplate) mis-link fixed in engine/template strings; photos pending — the moat |
| Job templates (5 seed jobs) | ✅ shipped | 2026-08-28 · 5/5 (PRD launch scope): aire mini-split (4 fixtures), ducha/calentador (3), tomacorriente 240 V (3), tomas/iluminación (3), alimentador a bodega (3: caso de caída larga con EGC subido por 250.122(B), toggle Cu/Al, GEC + 2 varillas por 250.32/250.66); todas con goldens a mano + sweep de alcanzabilidad en CI; precios de ítems nuevos pendientes (Vidrí cerró acceso — PRICES.md) |
| Price catalog (per-retailer, updatedAt-stamped) | 🚧 in progress | 2026-08-28 · 33 entries: 30 Vidrí (reproducible run) + 3 manual Freund/Vidrí (user-verified); ~45 ítems nuevos de plantillas 2–5 en `KNOWN_UNPRICED` (Vidrí ahora sirve challenge de Cloudflare a clientes no-navegador); cheapest-basket live on dual-priced lines |
| Price crawler (Playwright headful, multi-agent) | 📋 planned | 2026-08-28 · later this phase: rerun the reproducible PRICES.md procedure driving a REAL browser (Playwright headful) at human pace, optionally parallelized across agents, since plain HTTP clients now get challenged; scoped to retailers whose robots.txt permits crawling (Vidrí) — Freund/EPA declare an explicit AI opt-out (`ai-input=no`) and stay manual per the PRICES.md policy |

## Web app

| Feature | Status | Notes |
|---|---|---|
| Next.js PWA shell (Spanish-first, offline) | ✅ shipped | 2026-08-17 · installable offline PWA: content-hashed precache service worker (all routes + assets), manifest + maskable icons, query-string-aware navigation fallback — success criterion 4 |
| Mission-control panel (app shell + dashboard) | ✅ shipped | 2026-08-17 · sidebar nav + breadcrumb + dark-default theme toggle (print stays light); home = status strip (real package data), module launchers, quick-launch chips; blue/amber technical palette, Geist Mono numerals, WCAG AA both themes |
| Interactive calculators (live inputs, URL state) | ✅ shipped | 2026-08-28 · 5/5 live: calibre (sliders, gráfico, «fijar calibre»), relleno de tubería (mín/verificar, niple, Nota 7), tierra (T250.122 + 250.122(B)), cajas (mín/verificar, desglose), carga (métodos estándar y opcional lado a lado, aparatos con presets, encadena a calibre y a acometida). 2026-08-31: el cálculo de carga ahora ENCABEZA con «¿Alcanza su acometida?» — corriente calculada vs. la ampacidad del calibre que el usuario declara instalado (`?ac=`/`?acm=`), con veredicto alcanza / va justa / no alcanza y el margen en amperios; «Acometida sugerida» pasó a «Mínimo que pide el código», porque el piso de 100 A de 230.79(C) la vuelve una constante en vivienda salvadoreña y no una respuesta. La comparación también entra en la memoria. 2026-08-31: el relleno de tubería deriva la tierra del circuito (EGC) desde el térmico por Tabla 250.122 (`?egc=`), y calibre encadena a tubería llevando el térmico; fix: los popovers del glosario dentro de un `<label>` ya no se cierran solos al abrirlos |
| A/C: temperatura de techo, watts y punteras | ✅ shipped | 2026-08-31 · opción de recorrido «Sobre el techo / entretecho» (default 55 °C, tratada como ubicación húmeda) y tope de temperatura ambiente subido de 50 a 65 °C — un condensador y su recorrido al mediodía pasan de largo el tope viejo; `typicalW` en los presets de A/C (850/1150/1650/2250/3400 W) mostrado como parámetro y en el selector, marcado «por verificar» con lint de provenance propio (`KNOWN_UNVERIFIED_W`), más campo manual de W porque aquí los equipos se especifican en vatios; punteras (terminales tubulares) en la lista de materiales, 6 por trabajo; aviso 110.3(B) en calibres #14/#12: el manual del fabricante puede pedir más grueso y es exigible |
| Job flows → configurable priced BOM | ✅ shipped | 2026-08-28 · schema-driven `TemplateRunner` renders any template (widgets, URL keys, defaults y disabled desde el schema; back-compat de URLs viejas con test); headline: «aire» → 4 preguntas → parámetros citados + BOM con precios re-cotizado en vivo |
| Rename: Puesta a tierra → «Tierra de equipos (EGC)» | ✅ shipped | 2026-08-31 · el nombre anterior se confundía con la puesta a tierra del sistema (GEC), que ahora vive en la calculadora de acometida |
| Tooltips backed by glossary | ✅ shipped | 2026-08-17 · Term ids compile-time checked against the data glossary; popovers (tap-friendly on mobile) show definición, sinónimos, inglés y artículos NEC; coverage CI-enforced (success criterion 5 v1); auto-linking (`GlossaryText`) wraps terms in labels/BOM/supuestos |
| Beginner-readable provenance (chips + supuestos) | ✅ shipped | 2026-08-17 · citation chips lead with the plain-Spanish reason («ajuste por agrupamiento»), full NEC cite on tap; correction/adjustment tables cited only when their factor ≠ 1; assumption prose rewritten plain with structured `citations` chips |
| Glossary page (`/glosario`) | ✅ shipped | 2026-08-17 · searchable by término/sinónimo es-SV/inglés, NEC chips per entry; linked from sidebar |
| Acometida y panel principal (`/calculadoras/acometida`) | ✅ shipped | 2026-08-31 · GEC por Tabla 250.66 con tope de varilla 250.66(A), tubería de protección del GEC por 250.64(B)/(E), línea de varillas (250.53); emite la deviation `no-main-disconnect` (off-code, citas 230.70/230.71/408.36) con la nota de práctica local del tablero sin principal; encadenada desde el cálculo de carga con `?a=minServiceA` |
| Avisos «fuera de norma» + sello «No cumple NEC» | ✅ shipped | 2026-08-31 · tres estados visualmente distintos: error rojo (no se puede calcular), desviación ámbar (se calculó y sale de norma) y supuesto apagado. `DeviationsPanel` arriba de los números en los 5 calculadores y el job flow, con nota de práctica local de El Salvador cuando aplica; `NonComplianceBadge` en toda tarjeta con un número de esa corrida; variantes `warning` en `Alert`/`Badge` (tokens que ya existían); se retiraron las alertas rojas ad-hoc (`doesNotSatisfy`, `limitExceeded`, `fitsNo`) que ya eran desviaciones a mano. Entradas: `NumberField` nunca descarta una tecla en silencio — cotas de sanidad se recortan con aviso «Se limitó a N», cotas de código (2 circuitos de cocina, 3 % de caída) se aceptan y marcan; los parsers de URL ya no botan el enlace entero por un token viejo |
| Memoria de cálculo (PDF) | ✅ shipped | 2026-08-30 · print-only document on all 5 job flows + `/calculadoras/carga` (PRD US-6 / success criterion 1): datos de entrada (marca «valor por defecto»), parámetros, detalle por llamada al motor (derrateo, «limitado por», relleno), supuestos, avisos, BOM con precios (`*` override, `†` stale), citas NEC como notas al pie numeradas, descargo y línea de firma; datos de proyecto opcionales URL-persistidos (`pj`/`cl`/`rp` reservados, test de colisión); `window.print()` sigue siendo el exportador (offline-safe, sin librerías). 2026-08-31: bloque «Desviaciones del NEC» (con la nota de práctica local como letra chica y las citas entrando al apéndice de notas al pie) y sello a todo lo ancho bajo el encabezado cuando la corrida no cumple; el bloque llegó también a la memoria de carga, que no tenía canal de avisos |

## Infrastructure

| Feature | Status | Notes |
|---|---|---|
| Homelab deployment (nec.mcalero.dev) | ✅ shipped | 2026-08-30 · CI builds linux/arm64 nginx image → public GHCR on main merges; box release = `apps/nec/deploy.sh` in homelab-infrastructure (pull + up); SW-safe cache headers; Cloudflare-proxied, LE cert at origin |

## Later ideas (build when needed)

Saved projects · recent calculations on the panel (localStorage `ea-recent`, cap 8) ·
NL job intake (LLM intent parser) · community contributions ·
AR app (AR measures, engine sizes) · motor circuits (Art. 430) · NEC-2008 dual citations
(El Salvador's legally adopted edition, per SIGET Acuerdo 294-E-2011).
